# Memo 会话交接

> 2026-07-13 23:05

## 当前状态

| 项目 | 状态 |
|------|------|
| WorkBuddy 导入 | **续传中**（16/76 完成，267/330 条记忆） |
| HanaAgent 导入 | 已完成（46 会话，Hanako） |
| 数据库 | v4，330+ 条记忆，`E:\memo\memo\data\memo.db` |
| 人格提炼 | 已完成基线（43 断言，9/10 维度，knowledge 维需重跑） |
| 看板 | `http://localhost:9120`（需 `start_all.bat` 启动） |

## 待续命令

```powershell
# 续传 WorkBuddy 导入（断点续传，自动跳过已完成的16个）
python -m memo.services workbuddy

# 导入完成后执行
python -c "from memo.core.engine import engine; engine.init(); engine.run_lifecycle()"
E:\memo\start_all.bat
```

## 待处理清单

1. **WorkBuddy 导入跑完** + lifecycle 合并去重
2. **docs 隐私清理**：`setup-guide.md` 暴露 API Key，`deploy-guide.md`/`SKILL.md`/`generate_docx.py` 有硬编码路径
3. **knowledge 维度重跑**：`python -c "from memo.persona.extractor import build_persona_baseline; ..."`
4. **后台线程修复**：当前 `start_import` 是同步的，MCP 调用会阻塞，需要改子进程模式

## 关键路径

| 什么 | 路径 |
|------|------|
| 项目根 | `E:\memo` |
| 数据库 | `E:\memo\memo\data\memo.db` |
| 配置文件 | `E:\memo\.env` |
| 看板 | `http://localhost:9120` |
| 一键启动 | `E:\memo\start_all.bat` |
| 一键停止 | `E:\memo\stop_all.bat` |
| 架构文档 | `E:\memo\docs\V0.6-人格引擎-架构设计.md` |
| 部署指南 | `E:\memo\docs\deploy-guide.md` |
| 新机部署 | `E:\memo\docs\setup-guide.md` |
| GitHub | `https://github.com/adoublegirl-dev/memo` |
